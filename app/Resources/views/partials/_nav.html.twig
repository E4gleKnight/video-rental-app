<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Vidéo Passé</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li><a href="{{ path('homepage') }}">Catalogue</a></li>
                <li><a href="{{ path('search_page') }}">Recherche</a></li>

            </ul>

            <ul class="nav navbar-nav navbar-right">
                {% if not is_granted('ROLE_CUSTOMER') %}
                    <li><a href="{{ path('customer_login_page') }}">Connexion</a></li>
                    <li><a href="{{ path('register_page') }}">Inscription</a></li>

                {% else %}
                    <li><a href="{{ path('customer_logout') }}">Déconnexion</a></li>
                    <li><a href="{{ path('customer_account') }}">Mon compte</a></li>
                {% endif %}
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>